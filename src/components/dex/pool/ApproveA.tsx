const ApproveA = ({ amount }: { amount: string }) => {
    return (
        <div className="bg-green-500 text-white py-2 px-6 rounded-lg shadow-md hover:bg-green-600 transition">
            <p>Approve A</p>
        </div>
    );
};

export default ApproveA;
